/**
 * ============================================================================
 *  secrets.example.h — ŞABLON (bu dosya git'e gönderilir / commit edilir)
 *  ----------------------------------------------------------------------------
 *  KULLANIM:
 *    1) Bu dosyayı aynı klasörde "secrets.h" olarak KOPYALAYIN:
 *         main/secrets.example.h  ->  main/secrets.h
 *    2) secrets.h içine GERÇEK WiFi adı ve şifrenizi yazın.
 *
 *  GÜVENLİK: secrets.h, .gitignore tarafından yok sayılır; yani gerçek WiFi
 *  bilgileriniz ASLA GitHub'a gitmez. Bu örnek dosyada gerçek bilgi BULUNMAZ.
 * ============================================================================
 */
#pragma once

#define WIFI_SSID  "WIFI_ADINIZ"     // <-- WiFi ağ adınız
#define WIFI_PASS  "WIFI_SIFRENIZ"   // <-- WiFi şifreniz
