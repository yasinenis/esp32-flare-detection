# ESP-IDF Build directory
build/

# ESP-IDF Configuration backups
sdkconfig.old

# Component Manager (if used)
dependencies.lock
managed_components/

# Python / Scripts artifacts
__pycache__/
*.py[cod]
*$py.class

# ============================================================================
#  GİZLİ KİMLİK BİLGİLERİ — GitHub'a ASLA gönderilmez
#  (secrets.example.h şablonu commit edilir; secrets.h gerçek bilgileri tutar)
# ============================================================================
secrets.h
main/secrets.h
**/secrets.h

# Diğer olası gizli dosyalar
.env
*.env
credentials.h

# IDE / makineye özel ayarlar (yerel IDF yolu, COM portu vb.)
.vscode/
